# Copy to .env. Never committed — .gitignore covers it, and this repository is public.
#
# Read only by `npm run calibrate`, through node's own --env-file-if-exists, so
# the key exists for the length of that one command and is present in no shell,
# no profile, and no other process on the machine.
ANTHROPIC_API_KEY=
