node_modules/
*.log
.DS_Store
reports/
.stryker-tmp/

# Generated by CI onto the `state` branch, never onto main.
docs/state.json
docs/state.html

# Secrets. The negation must follow the patterns it negates, or it is ignored.
.env
.env.*
!.env.example
